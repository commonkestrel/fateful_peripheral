[package]
name = "advanced"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["rlib", "cdylib"]

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
fateful_peripheral = { path = "../.." }
anyhow = "1.0"
minifb = "0.25"
